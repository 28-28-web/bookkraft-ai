import UpsellBanner from '@/components/UpsellBanner';

// After the tool output/result shows:
{result && (
  <>
    <ResultOutput result={result} />
    <UpsellBanner toolName="EPUB Validator" />
  </>
)}