import CalibreAlternativePage from './CalibreAlternativePage';

export const metadata = {
  title: 'Calibre Alternative for Indie Authors — BookKraft AI',
  description: 'Calibre is powerful but complicated. BookKraft AI runs in any browser, no install needed, starts at $4.99 one-time. Free EPUB Validator and Metadata Builder included.',
  alternates: {
    canonical: 'https://bookkraftai.com/calibre-alternative',
  },
};

export default function Page() {
  return <CalibreAlternativePage />;
}