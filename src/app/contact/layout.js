export const metadata = {
  title: 'Contact Us — BookKraft AI',
  description: 'Get in touch with the BookKraft AI team.',
};

export default function ContactLayout({ children }) {
  return children;
}
